{
  "name": "yepgent-site",
  "version": "0.10.0",
  "description": "yepgent.com \u2014 static site + Netlify Functions for subscribe / track / account.",
  "type": "module",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4"
  },
  "scripts": {
    "build:config": "node scripts/build-config.mjs"
  }
}
